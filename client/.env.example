# JobConnect Frontend Environment Variables
# Copy this file to .env.local and fill in your values
# 
# IMPORTANT: 
# - Never commit .env.local file to version control!
# - Next.js requires NEXT_PUBLIC_ prefix for client-side variables
# - These variables are embedded at build time

# ============================================
# GraphQL API Configuration
# ============================================
# GraphQL HTTP Endpoint
# 
# Local development:
#   http://localhost:4000/graphql
#
# Docker Compose (from host machine):
#   http://localhost:4000/graphql
#
# Production:
#   https://your-api-domain.com/graphql
#
# Note: Use http:// for local, https:// for production
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:4000/graphql

# ============================================
# WebSocket Configuration
# ============================================
# WebSocket Endpoint for GraphQL Subscriptions
# 
# Local development:
#   ws://localhost:4000/graphql
#
# Docker Compose (from host machine):
#   ws://localhost:4000/graphql
#
# Production (with HTTPS):
#   wss://your-api-domain.com/graphql
#
# Note: 
#   - Use ws:// for HTTP (local development)
#   - Use wss:// for HTTPS (production)
#   - The protocol must match your HTTP endpoint
NEXT_PUBLIC_WS_URL=ws://localhost:4000/graphql